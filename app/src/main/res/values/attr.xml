<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="ForegroundImageView">
        <attr name="android:foreground" />
    </declare-styleable>

    <attr name="badgeGravity">
        <!-- Push object to the top of its container, not changing its size. -->
        <flag name="top" value="0x30" />
        <!-- Push object to the bottom of its container, not changing its size. -->
        <flag name="bottom" value="0x50" />
        <!-- Push object to the left of its container, not changing its size. -->
        <flag name="left" value="0x03" />
        <!-- Push object to the right of its container, not changing its size. -->
        <flag name="right" value="0x05" />
        <!-- Push object to the beginning of its container, not changing its size. -->
        <flag name="start" value="0x00800003" />
        <!-- Push object to the end of its container, not changing its size. -->
        <flag name="end" value="0x00800005" />
    </attr>
    <attr name="badgePadding" format="dimension|reference"/>
    <attr name="badgeText" format="string|reference"/>
    <attr name="badgeColor" format="color|reference"/>

    <declare-styleable name="BadgedFourThreeImageView">
        <attr name="badgeGravity"/>
        <attr name="badgePadding"/>
        <attr name="badgeText"/>
        <attr name="badgeColor"/>
    </declare-styleable>

    <declare-styleable name="BadgedView">
        <attr name="badgeGravity"/>
        <attr name="badgePadding"/>
        <attr name="badgeText"/>
        <attr name="badgeColor"/>
    </declare-styleable>

    <declare-styleable name="CollapsibleCard">
        <attr name="cardTitle" format="string" />
        <attr name="cardDescription" format="string" />
    </declare-styleable>

    <attr name="lineHeightHint" format="dimension" />

    <declare-styleable name="BaselineGridTextView">
        <attr name="lineHeightMultiplierHint" format="float" />
        <attr name="lineHeightHint" />
        <attr name="maxLinesByHeight" format="boolean" />
        <attr name="android:textAppearance"/>
        <attr name="android:fontFamily"/>
    </declare-styleable>

</resources>